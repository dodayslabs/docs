meta {
  name: token
  type: http
  seq: 3
}

post {
  url: {{protocol}}://{{host}}/{{version}}/auth/token
  body: json
  auth: none
}

body:json {
  {
    "email": "customer@dodays.test",
    "password": "password",
    "device_name": "Bruno"
  }
}

script:post-response {
  bru.setEnvVar("token", res.body.token);
}
