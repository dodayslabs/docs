meta {
  name: list
  type: http
  seq: 1
}

get {
  url: {{protocol}}://{{host}}/{{version}}/providers?filter[user]=6
  body: none
  auth: bearer
}

query {
  filter[user]: 6
}

auth:bearer {
  token: {{token}}
}
