meta {
  name: password
  type: http
  seq: 3
}

patch {
  url: {{protocol}}://{{host}}/{{version}}/account/password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "current_password": "password",
    "new_password": "password",
    "new_password_confirmation": "password"
  }
}
