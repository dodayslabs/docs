meta {
  name: session-reminders
  type: http
  seq: 7
}

patch {
  url: {{protocol}}://{{host}}/{{version}}/account/session-reminders
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "provider_id": 1
  }
}
