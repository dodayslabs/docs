meta {
  name: marketing-consent
  type: http
  seq: 5
}

patch {
  url: {{protocol}}://{{host}}/{{version}}/account/marketing-consent
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "provider_id": 1
  }
}
