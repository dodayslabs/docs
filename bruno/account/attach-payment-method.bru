meta {
  name: attach-payment-method
  type: http
  seq: 4
}

post {
  url: {{protocol}}://{{host}}/{{version}}/account/attach-payment-method
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "provider_id": 1,
    "payment_method": "pm_123456"
  }
}
